/**
 * Template for lab test batch
 * @param {object} labTestBatch 
 */
function LabTestBatch(labTestBatch) {
    packageId = labTestBatch.PackageId;
    labTestBatchId = labTestBatch.LabTestBatchId;
    labTestBatchName = labTestBatch.LabTestBatchName;
}

module.exports = LabTestBatch;